---
import Head from "../components/Head.astro";
import Navigation from "../components/Navigation.astro";
import Footer from "../components/Footer/Footer.astro";

type Props = {
  isFooterAvailable?: boolean;
};
const { isFooterAvailable = true } = Astro.props;
---

<html lang="en">
  <Head />
  <body>
    <main>
      <Navigation />
      <slot />
      {isFooterAvailable && <Footer />}
    </main>
  </body>
</html>

<style>
  main {
    //max-width: 1900px;
    //margin: 0 auto;
  }
</style>
